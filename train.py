# Required libraries
import torch
from torch import  nn
from torch.optim import Adam
import torch.nn.functional as F
import argparse

# Loading the data
import dataloader

# Building a model 
import model_loader


def train(img_path, model):
    pass
